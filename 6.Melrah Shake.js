function melrahShake(input) {
    let pattern = input.pop()
    let array = input.join(``)
    let newArr = []
    // let asciiNum = []
    // for (let i = 0; i < array.length; i++) {
    //     asciiNum.push(array[i].charCodeAt(array[i]))

    // }
for(let i=0; i<array.length;i++){
    let i=
}
      
    let result = array.replace(new RegExp(pattern, 'g'), '');
    newArr.push(new RegExp(pattern, 'g'), '')
    console.log(result); // Output: alavibaby
    console.log(newArr);
   
}
melrahShake(['astalavista baby', 'sta']);
//melrahShake(['##mtm!!mm.mm*mtm.#','mtm']);
